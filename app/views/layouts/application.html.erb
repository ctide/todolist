<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Todolist</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= stylesheet_link_tag    "datepicker" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Todo List</a>
          <div class="nav-collapse">
            <ul class="nav">
              <li class="<%= @current_controller == 'index' ? 'active' : '' %>"><a href="/">Home</a></li>
              <li class="<%= @current_controller == 'api' ? 'active' : '' %>"><a href="/api">API</a></li>
            </ul>
            <% if user_signed_in? %>
              <ul class="nav pull-right">
                <li><%= link_to "Logout", :destroy_user_session, :method => :delete %></li>
              </ul>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">

        <%= yield %>

    </div> <!-- /container -->
  </body>
</html>

